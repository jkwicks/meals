Issue register

	1.	1Any way to optimise space used by heading at top of page where days are listed along with macros/cals & Prep.  The day names are then repeated in the meal swim lanes.  The cook sessions, days cooking line also seems to take a bit of space.  Currently there are issues with alignment between the days showing macros, and the swim lanes for meal plan cards.  This could be avoided if all info is in the same lanes/columns ?
	2.	Can all buttons/menus start from left panel ? 
	⁃	Generate current week button takes up space in header of meal plan
	⁃	Print pdf, mobile file, etc are taking up space and makes it hard to figure out where the app “controls” are ?
	⁃	Review as per best practices for user interface design.
	3.	Can w get days added to day name (eg. Mon (2 Jul) etc
	4.	No switch/regenerate available for batch cooking.  This would be required as the batch cooking meals might need to be swapped.  Include both auto regeneration (ai call), or swap for favourite.
	5.	Need to be able to view the recipe for batch cooking via clicking the name in the batch cooking card.
	6.	Today (date) control on left panel should probably be renamed to “Daily View” or something similar ?
	7.	Library - can clicking anywhere on card cause recipe popup to happen ?
	8.	Setting tab: 
	⁃	We seem to be moving away from health connect sync and have implemented garmin/cronometer sync.  Would like recommendations to see if server start causes sync process to happen.  As this just runs from last time, I assume if restart happens frequently, it won’t cause calls to sync targets as data is already present?
	⁃	Sync could have its own popup/page showing current sync status for each target and which days/dates are present/missing
	⁃	Calendar/location needs popup/page showing default locations ?  Or maybe a calendar view of location.  Use defaults to show “expected” location.  Add note to say no integration with google calendar yet - just defaults
	⁃	Workout needs own page to show details of expected workouts.  Allow for future recommendations / reload sessions, or any other analytic results.
	9.	Sync process grabs all data when —date is provided.  If a —date parameter is provided, I would expect only that date to be checked and downloaded if missing.  Idea is to reduce attempts to connect to sync targets to avoid rate limiting.
	10.	Propose solution for morning readiness checkin.  I have got a new Garmin watch - Fenix, so can now start looking at HRV data.
	11.	Issue with storage of downloaded Garmin/cronometer data.  Biometrics.json does not seem to have stored metrics correctly unless it’s stored in elsewhere ?  I would expect to see the sleep data that has been downloaded previously ?  I only recently started weighing again, so this is not fully populated - by sleep show be.
