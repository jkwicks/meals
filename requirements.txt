pydantic
instructor
openai
python-dotenv
nicegui
reportlab
requests

# API boundary (src/api.py, mounted onto NiceGUI's own FastAPI app) and its
# tests (tests/test_api.py, via fastapi.testclient.TestClient). Both already
# arrive transitively through nicegui; pinned explicitly here because this
# code now imports them directly rather than riding on nicegui's pull.
fastapi
httpx

# OneNote sync (src/integrations/onenote_graph_import.py). msal drives the
# device-code login against Microsoft Graph; beautifulsoup4 strips a fetched
# page's layout HTML down to the plain text import_external_recipe expects.
msal
beautifulsoup4

# Biometric sync (src/integrations/sync_service.py).
#
# garminconnect's API differs by line and the interpreter picks which one:
# 3.9 caps at 0.2.8 (token persistence via `.garth`), 3.10+ resolves 0.3.x
# (no `.garth`; `login(tokenstore)` persists them itself). GarminSyncService
# handles both, so this stays unpinned — but that is why, not an oversight.
garminconnect

# Cronometer publishes no API for individual accounts; cronometer-mcp drives
# the same GWT-RPC protocol the web app uses, rediscovering its build hashes
# per login rather than pinning them. Needs Python >= 3.11, which this project
# now has — before the 3.14 move it sat behind a `python_version` marker and
# ran from a separate `venv-cronometer/` interpreter driven as a subprocess.
# Both are gone; sync_service.py now imports it in-process unconditionally.
cronometer-mcp
