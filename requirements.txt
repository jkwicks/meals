pydantic
instructor
openai
python-dotenv
eval_type_backport
urllib3<2
nicegui
reportlab

# Biometric sync (src/integrations/sync_service.py).
garminconnect

# Cronometer has no public API for individual accounts; cronometer-mcp drives
# the same GWT-RPC protocol the web app uses. It requires Python >= 3.11 and
# this project runs on the macOS system Python 3.9 (see CLAUDE.md), so the
# marker is what keeps `pip install -r requirements.txt` from hard-failing
# here — on 3.9 pip skips the line and sync_service drives the package from a
# separate interpreter instead. Create that one with:
#     python3.11 -m venv venv-cronometer
#     ./venv-cronometer/bin/pip install cronometer-mcp
cronometer-mcp; python_version >= "3.11"
